const { closePage } = require("./utils")

const openProfile = async () => {

}

const openMessageWindow = async () => {

}

const sendMessage = async () => {

}

const messageSender = ([]) => {
  for () {
    openProfile()
    openMessageWindow()
    sendMessage()
    
    openProfile() //next profile
    ...
  }

  nexContactPage() // if fonfig <= whichPage => close browser
}